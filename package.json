{
  "name": "stocksteer",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.26.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "test:unit": "vitest run --project unit --passWithNoTests",
    "test:integration": "vitest run --project integration --passWithNoTests",
    "db:up": "docker compose up -d --wait",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d --wait",
    "db:verify": "docker compose exec -T postgres bash /db/verify-roles.sh",
    "db:roles": "docker compose exec -T postgres bash -c 'psql -v ON_ERROR_STOP=1 --single-transaction -U postgres -d $POSTGRES_DB -v migrator_pw=$MIGRATOR_PASSWORD -v runtime_pw=$APP_RUNTIME_PASSWORD -v purger_pw=$PURGER_PASSWORD -v expect_db=$POSTGRES_DB -f /db/roles.sql'"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.2.0",
    "eslint": "^10.8.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.67.0",
    "vitest": "^4.1.11"
  }
}
